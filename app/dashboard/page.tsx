export default function Dashboard(){
    // we probable need some sort of distinction during sign up, user needs to select whether they are a creator or a normal user 
    return <div>
        Hi from Dashboard
    </div>
}